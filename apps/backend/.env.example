# ─── App ──────────────────────────────────────────────────────────────────────
APP_ENV=development          # "development" or "production"
PORT=8080

# ─── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=postgres://postgres:password@127.0.0.1:5433/podevents?sslmode=disable

# ─── Spotify OAuth ────────────────────────────────────────────────────────────
# Get from: https://developer.spotify.com/dashboard → Create App
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
# Must match EXACTLY what you set in Spotify dashboard → Redirect URIs
SPOTIFY_REDIRECT_URL=http://localhost:8080/api/v1/auth/spotify/callback

# ─── Token Encryption ─────────────────────────────────────────────────────────
# AES-256 key for encrypting Spotify tokens in DB (base64-encoded, 32 bytes).
# Generate: make generate-encryption-key
# WARNING: changing this after storing tokens breaks all existing tokens.
TOKEN_ENCRYPTION_KEY=your_base64_encoded_32_byte_key_here

# ─── JWT ──────────────────────────────────────────────────────────────────────
JWT_EXPIRES_IN_HOURS=24
JWT_SECRET=your_random_jwt_secret_here # generate with openssl rand -base64 32

# ─── Telegram ──────────────────────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN= # get from: https://t.me/BotFather
TELEGRAM_WEBHOOK_SECRET= # Generate a random secret, e.g.: openssl rand -base64 32
# Public HTTPS URL for Telegram to reach the webhook (use ngrok in dev)
TELEGRAM_WEBHOOK_URL=TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/v1/webhooks/telegram
BOT_NAME= # The name of your bot as it appears in Telegram
